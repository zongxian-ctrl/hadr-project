# Generated reports — the morning routine rebuilds these; do not commit churn.
reports/
*.sitrep.html

# The gate writes this each run as input for the /sitrep model; not a product.
briefing.json

# Output of the harness tutorial (harness/level5_dashboard.py); not a product.
harness-dashboard.html

# Generated landing page (harness/level8_capstone.py write_landing); not a product.
/index.html

# dashboard.html is the exception: it is the product, and it is committed.
!dashboard.html

# Secrets stay out of the repo and out of the agent's context.
.env
.env.*

# Language debris — extend for whichever toolchain you pick in CLAUDE.md.
__pycache__/
.venv/
node_modules/
.DS_Store
